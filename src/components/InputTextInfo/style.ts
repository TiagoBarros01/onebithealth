import { StyleSheet } from "react-native";

export const InputStyles = StyleSheet.create({
  Container: {
    width: '100%',

    marginBottom: 30,
  },
  Text: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#222'
  }
})