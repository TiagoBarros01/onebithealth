export interface ThemeProps {
  title: string;

  colors: {
    background: string;
    textColor: string;
    inputColor: string;
    lightBackground: string;
    listBackground: string;
    red: string;
    white: string;
  };
}
