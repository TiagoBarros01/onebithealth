export interface ThemeProps {
  title: string;

  colors: {
    background: string;
    textColor: string;
    inputColor: string;
    lightBackground: string;
    red: string;
    white: string;
  };
}
