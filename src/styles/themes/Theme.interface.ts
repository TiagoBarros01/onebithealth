export interface DefaultTheme {
  title: string;

  background: string;
  textColor: string;
  inputColor: string;
  lightBackground: string;
  listBackground: string;
  red: string;
  white: string;
}

export interface Theme {
  colors: DefaultTheme
}
