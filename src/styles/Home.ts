import { StyleSheet } from "react-native";

export const HomeStyles = StyleSheet.create({
  Area: {
    flex: 1,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center'
  }
})